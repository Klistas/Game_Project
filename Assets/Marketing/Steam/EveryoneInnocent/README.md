# Everyone Innocent Steam Marketing Assets

This folder is intentionally empty until the external gate passes.

Use `Assets/Games/_Commercial/SteamAssetChecklist.csv` as the asset tracker.

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Metaverse\GamePrototypeProject\Tools\ValidateSteamAssets.ps1"
```
